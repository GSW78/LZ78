#ifndef Test
#include <string>
#define MAX_LENGTH 2000
class Test
{
private:
    
    bool checkChars();
    bool checkAll();
public:
    Test(std::string input);
    bool checkSize();
      
};
#endif